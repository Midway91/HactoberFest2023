// SPDX-License-Identifier: GPL-3.0

pragma solidity >=0.8.2 <0.9.0;
//Integers
//            int(+,-)(8bit-256bit)                                  uint(+)(8bit-256bit)
            //                  Signed  and unsigned integers can be various sizes
            //  int8 to int256                                           uint8 to uint256
            //  int alias to int256                                      uint alias to uint256
            //                   By default and int and uint initialized to 0
            //                             Range
            //  int8:--128 to +127                                        uint8:-0 to 255
            //  -2^(n-1) to 2^(n-1)-1                                     0 to 2^(n)-1
//Operators
//comparisions:-<=,<,==,!=,>=,>(evaluate to bool)
//Bit Operators:- &,|,^(Bitwise Exclusive or),~(bitwise negotiation)
//Arthematic operators:- +,-,unary -,unary +,+,*,/,%(remainder),**(exponentiation),<<(left shift),>>(right shift)