def is_whole_number(number):
    if isinstance(number, float):
        return number.is_integer()
    elif isinstance(number, int):
        return True
    else:
        return False

# Example usage:
num = 42
result = is_whole_number(num)
print(f"{num} is a whole number: {result}")
